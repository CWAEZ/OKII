---
- job:
    name: "{job-name}"
    display-name: "{job-display-name}"
    description: "{job-description}"
    builders:
      - shell: |
          #!/usr/local/bin/runbld --redirect-stderr "$WORKSPACE/.ci/scripts/run-gradle.sh {gradle-args}"
    publishers:
      # Notify homer
#      - postbuildscript:
#          builders:
#            - role: SLAVE
#              build-on:
#                - SUCCESS
#                - FAILURE
#                - UNSTABLE
#              build-steps:
#                - shell: |
#                    curl -sS -X POST \
#                      -H "Content-Type: text/plain" \
#                      --data "$BUILD_URL" \
#                      "https://homer.app.elstc.co/webhook/jenkins/build-finished" || true
